{
  "name": "aeris-weather-api",
  "version": "1.0.0",
  "description": "A modern weather dashboard built with vanilla JavaScript and SASS",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "sass public/css/style.scss public/css/style.css --style compressed",
    "build:dev": "sass public/css/style.scss public/css/style.css --watch",
    "start": "node server.js",
    "dev": "npm run build:dev & nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "weather",
    "dashboard",
    "sass",
    "javascript",
    "api",
    "express",
    "modern-ui"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "sass": "^1.91.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
